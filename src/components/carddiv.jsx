export default function Carddiv({difficulty}){
    let ncards=0;
    if(difficulty=="easy")ncards=6;
    else if(difficulty=="medium")ncards=12;
    else ncards=20;
    return(
        <>
        </>
    )
}